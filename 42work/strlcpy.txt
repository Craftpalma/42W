size_t ft_strlcpy(char *dst, const char *src, size_t dstsize)
{
    size_t i;

    for (i = 0; i < dstsize - 1 && src[i] != '\\0'; i++)
    {
        dst[i] = src[i];
    }

    dst[i] = '\\0';

    while (src[i] != '\\0')
    {
        i++;
    }

    return i;
}